<UserControl x:Class="Parking.Views.SettingsView"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
              xmlns:conv="clr-namespace:Parking.Core.Converters"
              mc:Ignorable="d"
              d:DesignHeight="700" d:DesignWidth="1000">

    <UserControl.Resources>
        <conv:CurrencyConverter x:Key="CurrencyConv"/>
        <conv:VehicleTypeToBadgeColorConverter x:Key="VehicleBadgeConv"/>
        <conv:VehicleTypeToStringConverter x:Key="VehicleTypeStrConv"/>
        <conv:BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Configuración del Sistema y Tarifas" Style="{StaticResource TextHeaderMedium}"/>
            <TextBlock Text="Configure la capacidad de las instalaciones y la estructura tarifaria por categoría" Style="{StaticResource TextBodySecondary}" Margin="0,4,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.4*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource ModernCard}" Margin="0,0,16,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="ESTRUCTURA DE TARIFAS POR VEHÍCULO" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource BrushTextMuted}" Margin="0,0,0,12"/>

                        <ItemsControl ItemsSource="{Binding Rates}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource BrushSurfaceElevated}"
                                            BorderBrush="{DynamicResource BrushBorderSubtle}"
                                            BorderThickness="1"
                                            CornerRadius="10"
                                            Padding="16"
                                            Margin="0,0,0,12">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="1.5*"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding DisplayName}" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource BrushTextPrimary}"/>
                                                <TextBlock Text="{Binding VehicleType, Converter={StaticResource VehicleTypeStrConv}}" FontSize="11" Foreground="{DynamicResource BrushTextMuted}"/>
                                            </StackPanel>

                                            <StackPanel Grid.Column="1" Margin="6,0">
                                                <TextBlock Text="POR HORA ($)" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource BrushTextMuted}" Margin="0,0,0,4"/>
                                                <TextBox Text="{Binding HourlyRate, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Padding="8,6" FontSize="13"/>
                                            </StackPanel>

                                            <StackPanel Grid.Column="2" Margin="6,0">
                                                <TextBlock Text="FRACCIÓN ($)" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource BrushTextMuted}" Margin="0,0,0,4"/>
                                                <TextBox Text="{Binding FractionRate, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Padding="8,6" FontSize="13"/>
                                            </StackPanel>

                                            <StackPanel Grid.Column="3" Margin="6,0,0,0">
                                                <TextBlock Text="GRACIA (MIN)" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource BrushTextMuted}" Margin="0,0,0,4"/>
                                                <TextBox Text="{Binding GracePeriodMinutes, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Padding="8,6" FontSize="13"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource ModernCard}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="PARÁMETROS DE LA INSTALACIÓN" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource BrushTextMuted}" Margin="0,0,0,12"/>

                        <Border Background="{DynamicResource BrushSurfaceElevated}"
                                BorderBrush="{DynamicResource BrushBorderSubtle}"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="16"
                                Margin="0,0,0,18">
                            <StackPanel>
                                <TextBlock Text="Capacidad Total de Cupos (Plazas)" FontWeight="SemiBold" FontSize="13" Foreground="{DynamicResource BrushTextPrimary}" Margin="0,0,0,4"/>
                                <TextBlock Text="Máximo permitido de vehículos estacionados simultáneamente" FontSize="11" Foreground="{DynamicResource BrushTextSecondary}" Margin="0,0,0,10"/>
                                <TextBox Text="{Binding TotalCapacity, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource ModernTextBox}"
                                         FontWeight="Bold"
                                         FontSize="16"/>
                            </StackPanel>
                        </Border>

                        <Border Background="{DynamicResource BrushInputBackground}"
                                BorderBrush="{DynamicResource BrushBorderSubtle}"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="16"
                                Margin="0,0,0,24">
                            <StackPanel>
                                <TextBlock Text="POLÍTICAS DEL PARQUEADERO" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource BrushTextMuted}" Margin="0,0,0,8"/>
                                <TextBlock Text="• Periodo de gracia: salida sin costo si la duración está dentro del umbral." FontSize="11" Foreground="{DynamicResource BrushTextSecondary}" Margin="0,0,0,6"/>
                                <TextBlock Text="• Los intervalos de cobro se redondean a la hora transcurrido el tiempo de gracia." FontSize="11" Foreground="{DynamicResource BrushTextSecondary}" Margin="0,0,0,6"/>
                                <TextBlock Text="• Los comprobantes de caja se archivan automáticamente en la auditoría diaria." FontSize="11" Foreground="{DynamicResource BrushTextSecondary}"/>
                            </StackPanel>
                        </Border>

                        <Button Command="{Binding SaveSettingsCommand}"
                                Style="{StaticResource ModernButton}"
                                Height="44"
                                Margin="0,0,0,10">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <TextBlock Text="Guardar Configuración" Foreground="{DynamicResource BrushTextWhite}" FontWeight="Bold"/>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>

                        <Button Content="Restablecer Valores"
                                Command="{Binding ResetDefaultsCommand}"
                                Style="{StaticResource SecondaryButton}"
                                Height="40"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
