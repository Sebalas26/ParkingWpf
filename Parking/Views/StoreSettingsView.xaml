<UserControl x:Class="Parking.Views.StoreSettingsView"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
              xmlns:conv="clr-namespace:Parking.Core.Converters"
              mc:Ignorable="d"
              d:DesignHeight="700" d:DesignWidth="1000">

    <UserControl.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Gestión de Almacenes Aliados" Style="{StaticResource TextHeaderMedium}"/>
            <TextBlock Text="Administre los comercios aliados vinculados a convenios y descuentos de parqueadero" Style="{StaticResource TextBodySecondary}" Margin="0,4,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1.5*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource ModernCard}" Margin="0,0,16,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="DATOS DEL COMERCIO ALIADO" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource BrushTextMuted}" Margin="0,0,0,16"/>

                        <StackPanel Margin="0,0,0,14">
                            <TextBlock Text="Nombre del Comercio *" Style="{StaticResource TextBodySecondary}" Margin="0,0,0,6"/>
                            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,14">
                            <TextBlock Text="NIT / Identificación Tributaria *" Style="{StaticResource TextBodySecondary}" Margin="0,0,0,6"/>
                            <TextBox Text="{Binding TaxId, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,14">
                            <TextBlock Text="Teléfono de Contacto (Opcional)" Style="{StaticResource TextBodySecondary}" Margin="0,0,0,6"/>
                            <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>

                        <CheckBox Content="Comercio Activo para Convenios"
                                  IsChecked="{Binding IsActive}"
                                  Foreground="{DynamicResource BrushTextPrimary}"
                                  FontSize="13"
                                  Margin="0,6,0,20"/>

                        <Border Visibility="{Binding HasFeedback, Converter={StaticResource BoolToVis}}"
                                CornerRadius="8"
                                Padding="12"
                                Margin="0,0,0,16">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="{DynamicResource BrushSuccessBg}"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource BrushSuccess}"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSuccessFeedback}" Value="False">
                                            <Setter Property="Background" Value="{DynamicResource BrushDangerBg}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource BrushDanger}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="{Binding FeedbackMessage}" FontSize="12" TextWrapping="Wrap">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{DynamicResource BrushSuccess}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSuccessFeedback}" Value="False">
                                                <Setter Property="Foreground" Value="{DynamicResource BrushDanger}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Border>

                        <Button Command="{Binding SaveStoreCommand}"
                                Style="{StaticResource ModernButton}"
                                Height="44"
                                Margin="0,0,0,10">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <TextBlock Text="Guardar Comercio" Foreground="{DynamicResource BrushTextWhite}" FontWeight="Bold"/>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>

                        <Button Content="Limpiar Formulario"
                                Command="{Binding ClearFormCommand}"
                                Style="{StaticResource SecondaryButton}"
                                Height="38"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource ModernCard}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="ALMACENES REGISTRADOS" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource BrushTextMuted}" Margin="0,0,0,14"/>

                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding Stores}"
                              SelectedItem="{Binding SelectedStore}"
                              Style="{StaticResource ModernDataGrid}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="NOMBRE DEL COMERCIO" Binding="{Binding Name}" Width="1.5*"/>
                            <DataGridTextColumn Header="NIT / IDENTIFICACIÓN" Binding="{Binding TaxId}" Width="1.2*"/>
                            <DataGridTextColumn Header="TELÉFONO" Binding="{Binding PhoneNumber}" Width="*"/>
                            <DataGridTemplateColumn Header="ESTADO" Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="4" Padding="8,3" HorizontalAlignment="Center">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="{DynamicResource BrushSuccessBg}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                            <Setter Property="Background" Value="{DynamicResource BrushDangerBg}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock FontSize="11" FontWeight="Bold">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Text" Value="ACTIVO"/>
                                                        <Setter Property="Foreground" Value="{DynamicResource BrushSuccess}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                                <Setter Property="Text" Value="INACTIVO"/>
                                                                <Setter Property="Foreground" Value="{DynamicResource BrushDanger}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
